# No hover effects by default

Interactive elements are static — no colour changes, transforms, or animations on hover (exceptions: modal open/close transitions, expandable accordions). This was a deliberate call to prevent AI-generated components from accumulating unnecessary interactive states. Hover effects may be added in future, but each one should be a conscious decision, not a default. When in doubt, leave it out.
