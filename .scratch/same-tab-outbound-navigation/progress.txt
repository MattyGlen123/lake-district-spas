
## 01-ga-tracker-delay-navigation — completed 2026-05-19

Changes:
- `src/components/GoogleAnalytics.tsx`: added `e.preventDefault()` + 200ms `setTimeout(() => window.location.href = href)` for HTTP/HTTPS external links; protocol handlers (mailto/tel) still just fire dataLayer event
- `src/components/GoogleAnalytics.test.ts`: updated `initializeClickTracker` helper to match; added 5 new tests in "Delayed same-tab navigation" describe block using `vi.useFakeTimers()` and a `window.location` stub

Gotchas:
- `mockHrefSetter` must be typed as `(v: string) => void` (not `ReturnType<typeof vi.fn>`) to avoid TS2348 "not callable" error inside the `set href` accessor
- `vi.useFakeTimers()` / `vi.useRealTimers()` scoped to inner describe block — outer tests unaffected

## 02-bookvistcta-cleanup — completed 2026-05-19

Changes:
- `src/components/BookVisitCTA.tsx`: removed `target="_blank"`, `rel="noopener noreferrer"`, and `ExternalLink` icon from all 4 `<a>` elements (hotel, day pass, treatment, website fallback); removed `ExternalLink` from import
- `src/components/BookVisitCTA.test.tsx`: updated existing `target`/`rel` test to assert their absence instead

Gotchas: none

## 03-day-passes-cleanup — completed 2026-05-19

Changes:
- `src/components/DayPasses.tsx`: removed `target`/`rel` and `ExternalLink` icon from "Book Day Pass" CTA, "More info" inline link, and "Book Pass" button; removed ExternalLink import
- `src/components/DayPassCard.tsx`: same — "More info" and "Book Pass" links cleaned; ExternalLink import removed
- Email Us links had no `target`/`rel` in either file — no change needed there

Gotchas: DayPassCard has no dedicated test file; checked via full test run
